#include <stdlib.h>
#include <stdio.h>
#include <time.h>

int main()
{
  srand(time(NULL));
  printf("1000, 0.3, 0.7, 245, 0.4\n");
  int ind;
  for (ind = 0; ind < 1000; ind++)
  {
    printf("%d, %d, %d, %d, %d, %d, %d, %d, %d\n",ind + 1,rand() % 100,rand() % 100,rand() % 100,rand() % 100,rand() % 100,rand() % 100,rand() % 100,rand() % 100);
  }
  
  
  return 0;
}